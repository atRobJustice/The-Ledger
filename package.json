{
  "name": "ledger",
  "version": "1.0.0",
  "description": "Vampire: The Masquerade Character Sheet",
  "scripts": {
    "sass": "sass --watch scss:css",
    "sass:build": "sass scss:css --style compressed"
  },
  "devDependencies": {
    "sass": "^1.71.1"
  }
} 