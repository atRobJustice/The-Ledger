{
  "name": "the-ledger",
  "version": "1.3.0.1",
  "description": "Vampire: The Masquerade Character Sheet",
  "scripts": {
    "sass": "sass --watch scss:css",
    "sass:build": "sass scss:css --style compressed --no-source-map",
    "progeny:build": "node scripts/build-progeny.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "sass": "^1.71.1"
  }
} 